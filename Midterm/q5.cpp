#include <iostream>
#include <fstream>
#include <cstdlib>
#include <ctime>
using namespace std;

int getinput;
{
int a
cin >> a 
return a
}
void issame (int a, int b)
{
if (a==b)
break
}
int division ( int a, int b)
{
if (a > b)
    result = (a/b
    else
    result = (a/b);
}
int filewrite

int main()
{
    ofstream myfile;
    int n1, n2, result;
    n1 = getinput;
    n2 = getinput;
    issame (n1, n2);
    myfile.open("question5.txt");
    if (n1 > n2)
    result = (n1/n2)
    else
    result = (n2/n1);
else
if (n1 == n2)
exit;

    myfile.close();
    return 0;
}
